<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.djrapp.quizbowl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.ChooseTeamActivity"></activity>
        <activity android:name=".activities.CreateTeamActivity"></activity>
        <activity android:name=".activities.LobbyActivity"></activity>
        <activity android:name=".activities.ActiveGameActivity"></activity>
        <activity android:name=".activities.GameMasterActivity"></activity>
        <activity android:name=".activities.CreateGameActivity"></activity>
        <activity android:name=".activities.GameMasterLobbyActivity"></activity>
        <activity android:name=".activities.EndGameActivity"></activity>
    </application>

    <uses-permission
        android:required="true"
        android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission
        android:required="true"
        android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission
        android:required="true"
        android:name="android.permission.INTERNET"/>

</manifest>